export type userData = {
    handle: string;
    Name: string;
    rating: number;
    rank: string;
    country: string;
    maxRating: number;
};

export type userInfo = Array<{ name: string; value: string | number }>;
